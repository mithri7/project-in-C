#include <iostream>
#include<cmath>

using namespace std;

int main()
{
	int n;
	cin>>n;
	for(int j=0 ; j<2*n ; ++j)
	{
		for(int i=0 ; i<abs(n-j) ; ++i)
			cout<<" ";
		for(int i=0 ; i<2*n-2*abs(n-j) ; ++i)
			cout<<"*";
		cout<<endl;
	}
    return 0;
}
